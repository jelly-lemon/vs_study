VS 运行多个单独 main 程序不方便。为了运行某个 main 程序，得把其它文件排序在外（右键某 cpp 文件，然后：从此项目中排除）。<br/>

vs_study.sln 是解决方案信息文件，vs_study.vcxproj 是项目信息文件。
一个解决方法可以包含多个项目。

如果项目是控制台程序，则子系统应该为：控制台。
如果是 win32 程序，子系统应该为：窗口。
如果不匹配，则会报错。
设置方法：右键项目->属性->配置属性->链接器->系统->子系统